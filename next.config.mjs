// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['placehold.co'], // Add the allowed external image domain
    },
  };
  
  export default nextConfig;